# FareScope Server

The server is a modular Python application with separate process entrypoints for HTTP APIs, scheduled dispatch, browser collection, analysis, and notification delivery.

Only the HTTP liveness foundation is implemented at this stage. Database models, migrations, collectors, and task workflows remain tracked in `../docs/PROJECT_PLAN.md`.
